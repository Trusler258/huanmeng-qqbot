# ── 版本号配置 ─────────────────────────────────────────────
# v2.1.11(2026.9.12): 版本号独立成配置，不再从 update_log.md 正则解析。
# 升级版本时改这里即可；update_log.md 中的 ## vX.Y.Z 标题只作文档。
# 注意: 提示词 self_awareness 的 ${version} 与 /~changelog 均以此为唯一来源。

[version]
current = "v2.3.45"